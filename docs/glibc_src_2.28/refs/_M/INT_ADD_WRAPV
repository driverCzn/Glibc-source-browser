<dec f='glibc_src_2.28/posix/regex_internal.h' l='50'/>
<use f='glibc_src_2.28/posix/regexec.c' l='332' u='c'/>
<doc f='glibc_src_2.28/posix/regex_internal.h' l='44'>/* True if adding the nonnegative Idx values A and B would overflow.
   If false, set *R to A + B.  A, B, and R may be evaluated more than
   once, or zero times.  Although this is not a full implementation of
   Gnulib INT_ADD_WRAPV, it is good enough for glibc regex code.
   FIXME: This implementation is a fragile stopgap, and this file would
   be simpler and more robust if intprops.h were migrated into glibc.  */</doc>
