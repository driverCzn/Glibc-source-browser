<dec f='glibc_src_2.31/resolv/netdb.h' l='690' type='int getaddrinfo_a(int __mode, struct gaicb **restrict __list, int __ent, struct sigevent *restrict __sig)'/>
<doc f='glibc_src_2.31/resolv/netdb.h' l='682'>/* Enqueue ENT requests from the LIST.  If MODE is GAI_WAIT wait until all
   requests are handled.  If WAIT is GAI_NOWAIT return immediately after
   queueing the requests and signal completion according to SIG.

   This function is not part of POSIX and therefore no official
   cancellation point.  But due to similarity with an POSIX interface
   or due to the implementation it is a cancellation point and
   therefore not marked with __THROW.  */</doc>
<def f='glibc_src_2.31/resolv/getaddrinfo_a.c' l='37' ll='182' type='int getaddrinfo_a(int mode, struct gaicb ** list, int ent, struct sigevent * sig)'/>
