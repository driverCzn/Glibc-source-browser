<def f='include/c++/7/bits/postypes.h' l='177' ll='183' type='fpos&lt;_StateT&gt; std::fpos::operator+(std::streamoff __off) const'/>
<doc f='include/c++/7/bits/postypes.h' l='171'>// The standard requires that this operator must be defined, but
      // defines its semantics only in terms of operator-. In this
      // implementation it constructs a copy of *this, adds the
      // argument to that copy using operator+= and then returns the
      // copy.
      /// Add position and offset.</doc>
