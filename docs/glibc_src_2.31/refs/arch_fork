<def f='glibc_src_2.31/sysdeps/unix/sysv/linux/arch-fork.h' l='31' ll='54' type='pid_t arch_fork(void * ctid)'/>
<doc f='glibc_src_2.31/sysdeps/unix/sysv/linux/arch-fork.h' l='24'>/* Call the clone syscall with fork semantic.  The CTID address is used
   to store the child thread ID at its locationm, to erase it in child memory
   when the child exits, and do a wakeup on the futex at that address.

   The architecture with non-default kernel abi semantic should correctlly
   override it with one of the supported calling convention (check generic
   kernel-features.h for the clone abi variants).  */</doc>
<use f='glibc_src_2.31/sysdeps/nptl/fork.c' l='76' u='c' c='__libc_fork'/>
