<dec f='glibc_src_2.23/pwd/pwd.h' l='102' type='int putpwent(const struct passwd *restrict __p, FILE *restrict __f)'/>
<doc f='glibc_src_2.23/pwd/pwd.h' l='96'>/* Write the given entry onto the given stream.

   This function is not part of POSIX and therefore no official
   cancellation point.  But due to similarity with an POSIX interface
   or due to the implementation it is a cancellation point and
   therefore not marked with __THROW.  */</doc>
<use f='glibc_src_2.23/nss/getent.c' l='570' u='c' c='print_passwd'/>
<def f='glibc_src_2.23/pwd/putpwent.c' l='29' ll='64' type='int putpwent(const struct passwd * p, FILE * stream)'/>
<doc f='glibc_src_2.23/pwd/putpwent.c' l='25'>/* Write an entry to the given stream.  This must know the format of
   the password file.  If the input contains invalid characters,
   return EINVAL, or replace them with spaces (if they are contained
   in the GECOS field).  */</doc>
