<dec f='glibc_src_2.25/libidn/iconvme.h' l='22' type='char * iconv_string(const char * string, const char * from_code, const char * to_code)'/>
<def f='glibc_src_2.25/libidn/iconvme.c' l='59' ll='171' type='char * iconv_string(const char * str, const char * from_codeset, const char * to_codeset)'/>
<doc f='glibc_src_2.25/libidn/iconvme.c' l='52'>/* Convert a zero-terminated string STR from the FROM_CODSET code set
   to the TO_CODESET code set.  The returned string is allocated using
   malloc, and must be dellocated by the caller using free.  On
   failure, NULL is returned and errno holds the error reason.  Note
   that if TO_CODESET uses \0 for anything but to terminate the
   string, the caller of this function may have difficulties finding
   out the length of the output string.  */</doc>
<use f='glibc_src_2.25/libidn/toutf8.c' l='108' u='c' c='stringprep_convert'/>
