<def f='glibc_src_2.24/sunrpc/svcauth_des.c' l='499' ll='586' type='int authdes_getucred(const struct authdes_cred * adc, uid_t * uid, gid_t * gid, short * grouplen, gid_t * groups)'/>
<use f='glibc_src_2.24/sunrpc/svcauth_des.c' l='587' c='__EI_authdes_getucred'/>
<doc f='glibc_src_2.24/sunrpc/svcauth_des.c' l='493'>/*
 * Map a des credential into a unix cred.
 * We cache the credential here so the application does
 * not have to make an rpc call every time to interpret
 * the credential.
 */</doc>
