<dec f='glibc_src_2.24/sysdeps/unix/sysv/linux/x86_64/lowlevellock.h' l='308'/>
<doc f='glibc_src_2.24/sysdeps/unix/sysv/linux/x86_64/lowlevellock.h' l='303'>/* The kernel notifies a process which uses CLONE_CHILD_CLEARTID via futex
   wake-up when the clone terminates.  The memory location contains the
   thread ID while the clone is running and is reset to zero by the kernel
   afterwards.  The kernel up to version 3.16.3 does not use the private futex
   operations for futex wake-up when the clone terminates.  */</doc>
<use f='glibc_src_2.24/nptl/pthread_join.c' l='90' u='c'/>
