<dec f='glibc_src_2.30/sysdeps/generic/math_private_calls.h' l='91' type='double __gamma_product(double x, double x_eps, int n, double * eps)'/>
<doc f='glibc_src_2.30/sysdeps/generic/math_private_calls.h' l='84'>/* Compute the product of X + X_EPS, X + X_EPS + 1, ..., X + X_EPS + N
   - 1, in the form R * (1 + *EPS) where the return value R is an
   approximation to the product and *EPS is set to indicate the
   approximate error in the return value.  X is such that all the
   values X + 1, ..., X + N - 1 are exactly representable, and X_EPS /
   X is small enough that factors quadratic in it can be
   neglected.  */</doc>
<use f='glibc_src_2.30/sysdeps/ieee754/dbl-64/e_gamma_r.c' l='67' u='c' c='gamma_positive'/>
<use f='glibc_src_2.30/sysdeps/ieee754/dbl-64/e_gamma_r.c' l='84' u='c' c='gamma_positive'/>
<def f='glibc_src_2.30/sysdeps/ieee754/ldbl-96/gamma_product.c' l='32' ll='44' type='double __gamma_product(double x, double x_eps, int n, double * eps)'/>
<doc f='glibc_src_2.30/sysdeps/ieee754/ldbl-96/gamma_product.c' l='24'>/* Compute the product of X + X_EPS, X + X_EPS + 1, ..., X + X_EPS + N
   - 1, in the form R * (1 + *EPS) where the return value R is an
   approximation to the product and *EPS is set to indicate the
   approximate error in the return value.  X is such that all the
   values X + 1, ..., X + N - 1 are exactly representable, and X_EPS /
   X is small enough that factors quadratic in it can be
   neglected.  */</doc>
