<def f='glibc_src_2.29/malloc/dynarray-skeleton.c' l='373' macro='1' type='_Bool fork_handler_list_resize(struct fork_handler_list * list, size_t size)'/>
<doc f='glibc_src_2.29/malloc/dynarray-skeleton.c' l='367'>/* Change the size of *LIST to SIZE.  If SIZE is larger than the
   existing size, new elements are added (which can be initialized).
   Otherwise, the list is truncated, and elements are freed.  Return
   false on memory allocation failure (and mark *LIST as failed).  */</doc>
