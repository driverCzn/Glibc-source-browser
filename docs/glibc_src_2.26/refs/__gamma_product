<dec f='glibc_src_2.26/sysdeps/generic/math_private_calls.h' l='100' type='double __gamma_product(double x, double x_eps, int n, double * eps)'/>
<doc f='glibc_src_2.26/sysdeps/generic/math_private_calls.h' l='93'>/* Compute the product of X + X_EPS, X + X_EPS + 1, ..., X + X_EPS + N
   - 1, in the form R * (1 + *EPS) where the return value R is an
   approximation to the product and *EPS is set to indicate the
   approximate error in the return value.  X is such that all the
   values X + 1, ..., X + N - 1 are exactly representable, and X_EPS /
   X is small enough that factors quadratic in it can be
   neglected.  */</doc>
<use f='glibc_src_2.26/sysdeps/ieee754/dbl-64/e_gamma_r.c' l='64' u='c' c='gamma_positive'/>
<use f='glibc_src_2.26/sysdeps/ieee754/dbl-64/e_gamma_r.c' l='81' u='c' c='gamma_positive'/>
<def f='glibc_src_2.26/sysdeps/ieee754/ldbl-96/gamma_product.c' l='31' ll='43' type='double __gamma_product(double x, double x_eps, int n, double * eps)'/>
<doc f='glibc_src_2.26/sysdeps/ieee754/ldbl-96/gamma_product.c' l='23'>/* Compute the product of X + X_EPS, X + X_EPS + 1, ..., X + X_EPS + N
   - 1, in the form R * (1 + *EPS) where the return value R is an
   approximation to the product and *EPS is set to indicate the
   approximate error in the return value.  X is such that all the
   values X + 1, ..., X + N - 1 are exactly representable, and X_EPS /
   X is small enough that factors quadratic in it can be
   neglected.  */</doc>
