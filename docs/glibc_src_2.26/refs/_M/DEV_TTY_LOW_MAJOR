<dec f='glibc_src_2.26/sysdeps/unix/sysv/linux/device-nrs.h' l='37'/>
<doc f='glibc_src_2.26/sysdeps/unix/sysv/linux/device-nrs.h' l='32'>/* Pseudo tty slaves.  For Linux we use the Unix98 ttys.  We could
   also include the old BSD-style tty buts they should not be used and
   the extra test would only slow down correctly set up systems.  If a
   system still uses those device the slower tests performed (using
   isatty) will catch it.  */</doc>
