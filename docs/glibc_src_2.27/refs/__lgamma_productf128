<def f='glibc_src_2.27/sysdeps/ieee754/ldbl-128/lgamma_productl.c' l='30' macro='1' type='int __lgamma_productf128(int t, int x, int x_eps, int n)'/>
<doc f='glibc_src_2.27/sysdeps/ieee754/ldbl-128/lgamma_productl.c' l='23'>/* Compute the product of 1 + (T / (X + X_EPS)), 1 + (T / (X + X_EPS +
   1)), ..., 1 + (T / (X + X_EPS + N - 1)), minus 1.  X is such that
   all the values X + 1, ..., X + N - 1 are exactly representable, and
   X_EPS / X is small enough that factors quadratic in it can be
   neglected.  */</doc>
