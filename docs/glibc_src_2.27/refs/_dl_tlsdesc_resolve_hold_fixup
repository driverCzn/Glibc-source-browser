<def f='glibc_src_2.27/sysdeps/x86_64/tlsdesc.c' l='114' ll='131' type='void _dl_tlsdesc_resolve_hold_fixup(volatile struct tlsdesc * td, void * caller)'/>
<doc f='glibc_src_2.27/sysdeps/x86_64/tlsdesc.c' l='108'>/* This function is used to avoid busy waiting for other threads to
   complete the lazy relocation.  Once another thread wins the race to
   relocate a TLS descriptor, it sets the descriptor up such that this
   function is called to wait until the resolver releases the
   lock.  */</doc>
